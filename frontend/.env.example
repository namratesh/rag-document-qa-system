# Copy to .env and fill in real values. .env is gitignored.

# Backend API location -- the frontend builds its API base URL from these
# instead of hardcoding a host, e.g. http://127.0.0.1:8000.
VITE_API_IP=127.0.0.1
VITE_API_PORT=8000
