fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-multipart>=0.0.9

# PDF parsing
pypdf>=5.0.0

# Vector store (local, on-disk)
chromadb>=0.5.0

# LLM + embeddings, via OpenRouter's OpenAI-compatible API
openai>=1.40.0
requests>=2.32.0

# Tests
pytest>=8.0.0
